query GetTag($name: String!) {
  tag(name: $name) {
    name
    posts {
      edges {
        node {
          ...postPreview
        }
      }
      pageInfo {
        hasNextPage
        endCursor
      }
    }
  }
}
