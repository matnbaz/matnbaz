query GetPost($slug: String!) {
  postBySlug(slug: $slug) {
    id
    slug
    title
    image
    contentHtml
    publishedAt {
      formatted
      difference
    }
    author {
      id
      name
      username
      bio
    }
  }
}
