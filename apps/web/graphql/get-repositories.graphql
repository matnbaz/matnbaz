query GetRepositories(
  $after: String
  $searchTerm: String
  $languages: [String!]
  $order: RepoOrder
) {
  repositories(
    first: 8
    after: $after
    searchTerm: $searchTerm
    languages: $languages
    order: $order
  ) {
    edges {
      node {
        fullName
        limitedDescription
        stargazersCount
        forksCount
        openIssuesCount
        language {
          name
          color
        }
        owner {
          type
          platformId
        }
      }
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
