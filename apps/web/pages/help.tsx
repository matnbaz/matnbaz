import MainLayout from '../components/Layout/MainLayout';

const Help = () => {
  return (
    <MainLayout>
      <pre>
        WIP, this page will *probably* be something like you can help by helping
        maintaining the project, manually adding github user/orgs and donating
      </pre>
    </MainLayout>
  );
};

export default Help;
